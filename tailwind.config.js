

module.exports = {
    content: [
      "./src/**/*.{js,jsx}", // Ensures Tailwind scans all JS/JSX files in src/
    ],
    theme: {
      extend: {
        // Add custom styles here if needed
      },
    },
    plugins: [],
  }
  